import { map } from 'lodash';
import React, { useEffect, useState } from 'react';
import ReactDOM from 'react-dom';

function TimeEntry() {

    return (
        <div className="">


        </div>
    );
}

export default TimeEntry;

if (document.getElementById('timeEntry')) {
    ReactDOM.render(<TimeEntry />, document.getElementById('timeEntry'));
}
